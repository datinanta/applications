import { Component } from '@angular/core';
import { Router } from '@angular/router';

@Component({
  moduleId: module.id,
  selector: 'ng-nav-bar',
  templateUrl: './nav-bar.component.html'
})

export class NavBarComponent {
  private title = "DATINANTA";    
}
